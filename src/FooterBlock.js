import React from 'react';
const FooterBlock = () => {
    return (
        <div className="p-2 p-md-3 bg-dark text-white" >
            <b>Disclaimer</b>
            <p className=""><small>The best way to consume React-Bootstrap is via the npm package which you can install with npm (or yarn if you prefer).</small></p>
        </div>
    );
}

export default FooterBlock;